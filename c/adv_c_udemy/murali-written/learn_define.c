#include <stdio.h>
#define int_pointer int *
typedef char* char_ptr;

int_pointer chalk, cheese; // same as int *chalk, cheese;
char_ptr Bently, Rolls_Royce; // same as char* Bently and char* Rolls_Royce; so both Bently and Rolls_Royce are pointers to char

typedef int banana;

int main(){
    return 0;
}
