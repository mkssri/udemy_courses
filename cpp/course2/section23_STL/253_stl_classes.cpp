 /**************************
  * author : murali sriram
  * date   : 04/30/2024
  * ***********************/
#include<iostream>

using namespace std;

/*
 *
 * STL classes has: (All these classes are template classes) 
 * - template: since they are generic classes, can store any type of data
 * - container contains collection of data
 * - Algorithms: 
 *   search(), sort(), binary_search(), reverse, concat(), copy(), union(),
 *   intersection(), merge(), heap().
 * - Containers: 
 *   container classes that are available are: 
 *   vector : push_back(), pop_back(),insert(), remove(), size(), empty()
 *   list 
 *   forward_list
 *   deque
 *   priority_queue
 *   stack
 *   set
 *   multiset
 * - Iterators: accessing containers via iterators
 *
 */




int main()
{
    return 0;
}
